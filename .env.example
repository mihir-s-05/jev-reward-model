# Export these in your shell. The program intentionally does not auto-load .env.
TYPESAFE_API_KEY=replace-me
# A separate local or hosted OpenAI-compatible judge; no OpenAI account is required.
JUDGE_BASE_URL=http://localhost:8001/v1
JUDGE_MODEL=Qwen/Qwen3.5-9B
JUDGE_API_KEY=unused-for-local-server
