# Export these in your shell; no code reads a .env file automatically.
TYPESAFE_API_KEY=
# Optional auth for your separately served frozen Qwen judge.
QWEN_JUDGE_API_KEY=
# Use one exact Hub commit for actor and frozen judge across the comparison.
QWEN_REVISION=
